---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: example-app
  labels:
    app: example-app
spec:
  port:
    targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: edge
  to:
    kind: Service
    name: example-app
